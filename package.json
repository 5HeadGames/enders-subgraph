{
  "name": "endersgate-graph",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "prepare": "yarn codegen && yarn build"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.30.3",
    "@graphprotocol/graph-ts": "^0.27.0"
  }
}
